snapshot_id: discogs-2026-04
paths:
  raw_dir: data/raw/discogs
  staging_dir: data/staging
  clean_dir: data/clean
  analytics_dir: data/analytics
  published_duckdb: data/published/duckdb/discogs.duckdb
  manifests_dir: data/manifests
  logs_dir: data/logs
limits:
  parser_batch_size: 50000
  log_progress_every: 10000
  # Peak RSS cap (informational; exceeding emits a manifest warning, not a failure).
  peak_rss_cap_gib: 4
  # Above this row count per Parquet, DQ checks switch from in-memory to DuckDB SQL.
  dq_check_in_memory_threshold: 10000000
